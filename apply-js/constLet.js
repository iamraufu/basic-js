var name = 'Raufu';
if (name.length > 4) {
    name = 'Raufu Prezens';
}
console.log(name);

// Const use for not changing again - it can't be change later
const country = 'Bangladesh';

// let use for changing later -it could be changed later
let home = 'Rampura';
console.log(country);
home = 'Bonosree';
console.log(home);